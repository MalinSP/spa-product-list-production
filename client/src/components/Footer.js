import styled from "styled-components";

const Footer = () => {
  return <Wrapper>Scandiweb testTask</Wrapper>;
};

const Wrapper = styled.footer`
  height: 3rem;
  display: grid;
  place-items: center;
  color: #474839;
`;
export default Footer;
